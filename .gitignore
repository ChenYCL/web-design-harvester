node_modules/
out/
out-*/
spec/
.scratch/
*.log

# Internal planning doc — contains client project references; not for publication.
mvp.md

# Harvest artefacts land wherever --out points; these are the common names.
screenshot-*.png
shot-*.png

.DS_Store
# run output at repo root only — docs/rehearsal/ is committed
/rehearsal/
/tmp/
docs/.scratch/

# Rehearsal screenshots render a customer site — keep them local.
docs/rehearsal/*.png

# Built locally by `npm run extension:build`
extension/dist/
